# data221-assignment-1
Kein's submission for Assignment 1 - DATA 221

Question 1 - A program that multiplies consecutive integers from 1 until the product becomes greater than a given threshold
Question 2 - Define a function that receives a list of strings and returns a dictionary consisting of string length and parity of that length
Question 3 - Define a function that computes x^y, given a list of pairs (x,y), return the computation in another list, skips the pair if y is negative
Question 4 - Given a list of values and a value x, sort the list, find the first index where the value in the list is greater than or equal to x
Question 5 - Define a function with 2 arguments containing radii of 2 circles. Compute the area, and the percentage of the larger circle's area that the smaller circle covers. Makes sure that both radii are positive.
Question 6 - Define a function that receives a list of numbers and returns a dictionary containing the percentage of elements that is less than or equal to the key in the list
Question 7 - Convert seconds into hours, mins, seconds, AM/PM
Question 8 - Put data into a data frame, add a column using existing columns.

Fri Jan 30
